# NEMT 1.1.0-alpha.4 — compact form and startup movie comparison

- Compact the configuration form to a 760×648 client area at normal scaling, retaining all options and a small-screen/high-DPI scrolling fallback.
- Try Windows' standard MPEG driver before MSTS's legacy driver for the startup clip.
- Restore focus and application activation after movie playback when MSTS remains foreground; do not steal focus from another application.
- Enable the two movie comparison settings on Apply, with independent settings.ini overrides preserved on reapply.
- Record successful host validation of alpha.3's native red-signal alert and repeat suppression.

Automated movie, signal, timing, lifecycle, startup, bootstrap and mutation checks pass, as do supported-site and installer checks. Actual video rendering and keyboard recovery require the [host comparison](../docs/investigations/startup-movie.md). Stable 1.0.2 is unchanged.
