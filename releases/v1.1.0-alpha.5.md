# NEMT 1.1.0-alpha.5 — movie window rendering comparison

- Display the startup movie in a borderless window owned by MSTS, rather than embedded in the game's rendering window.
- Centre the movie and explicitly set its video destination rectangle while preserving aspect ratio.
- Add a startup-only geometry record when optional startup logging is enabled.
- Retain the host-validated keyboard recovery from alpha.4.

Window ownership/geometry, movie behavior, instruction-site, installer and native regression checks pass. Actual video rendering still needs the [windowed/fullscreen host comparison](../docs/investigations/movie-window-host-test.md). Stable 1.0.2 is unchanged.
