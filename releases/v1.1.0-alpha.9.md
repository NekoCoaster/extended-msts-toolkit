# NEMT 1.1.0-alpha.9 — simplified movie option

- Combine movie skipping and the keyboard option under **Skip startup movie (fixes keyboard control issue when loading into simulator)**.
- Remove the separate keyboard checkbox. Movie skipping passed host testing; standalone recovery did not resolve the latest host test. Further troubleshooting is closed.
- Rename the signal option to **Continue after passing a red signal (Resume after failure message)**.

The native DLL is unchanged. Configuration checks cover both combined-option states, skip-only installation and uninstall without changing train.exe. The compact layout and Select All remain available. Stable 1.0.2 is unchanged.
