# NEMT 1.1.0-alpha.7 — movie output-window assignment

- Explicitly assign the opened movie decoder to its final movie window before playback.
- Record decoder device, output-window query, assignment result and verification in the optional startup log.
- Keep the original movie fallback, skip handling and keyboard recovery.

This is a diagnostic comparison build. The host played audio but displayed no video with all four alpha.6 decoder/window combinations. Alpha.7 checks the decoder's output binding separately from its destination rectangle; it is not yet a confirmed host rendering fix.

Automated checks cover assignment success, command rejection, missing devices, disabled behavior, settings persistence and movie/window regressions. A live VM run accepted the assignment and returned normally after playback; its output handle already matched before reassignment. The screenshot arrived after the movie, so this run does not establish visual rendering success for alpha.7. Stable 1.0.2 is unchanged.

## Host check

1. Close MSTS, pull the testing branch, run NEMT.vbs and verify **1.1.0-alpha.7**. Apply with startup logging enabled.
2. Keep `SeparateMovieWindow=true` and `UseSystemMovieDecoder=true`. Apply adds `BindMovieOutput=true` automatically.
3. Launch once. Report whether the picture appears and share startup.log, especially its `OUTPUT` records. There is no need to repeat the four earlier combinations.

Set `BindMovieOutput=false` and restart to disable this comparison independently. Apply preserves the explicit setting.
