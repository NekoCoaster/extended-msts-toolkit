# NEMT 1.1.0-alpha.6 — startup movie regression investigation

- Defer movie window reparenting until the decoder has successfully opened the clip.
- Record the native movie preference and opening/playback stages in the optional startup log, including after normal startup logging finishes.
- Preserve the working keyboard recovery after finishing or skipping the movie.

Alpha.5 skipped the movie on the host. This build addresses the pre-open window change and improves diagnosis; visible playback still needs the [host comparison](../docs/investigations/movie-window-host-test.md). Native regression checks pass. Stable 1.0.2 remains unchanged.
