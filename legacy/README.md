# Legacy PowerShell frontend

`NEMT.ps1` and the original `NEMT.vbs` are retained here as the reference
implementation while the native Win32 frontend is validated feature-for-feature.
They are not the intended runtime frontend going forward.
