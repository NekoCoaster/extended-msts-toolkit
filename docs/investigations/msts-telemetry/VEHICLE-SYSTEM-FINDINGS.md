# Per-vehicle brake and force checkpoint

Executable verification: pass26 checks1757 exported instructions/6978 bytes. All disk bytes match the analysis image. Three instruction records differ at0x62ad6a..0x62ad6d: the function entry is a live jump patch followed by padding. This is one patched entry spanning three exported instructions, not three independent features. Coupling/force code below that entry is native static evidence; its runtime path must be treated as potentially wrapped. Other exported functions in the pass have no detected differences. See pass26-byte-verification.json.

Read-only sample vehicle-systems-running-01 contains the 23 stopped player vehicles during advancing simulation. The first AI had already lost its physical representation, so this sample does not validate AI brake/force population.

Lead cylinder pressure car+0x230=85.20245 PSI, pipe car+0x238=0. Last wagon cylinder=76.22182, pipe=0. Per-car values therefore carry information beyond the single cab display. No propagation timing or release transition was tested here.

Native writer0x6171f8 associates token0x40196 MaxBrakeForce with definition+0x918. Lead definition=140000 N; wagon=50000 N. The runtime force quantity car+0x22c was170404.9 and76221.82 respectively. It must not be clamped to the configured maximum in telemetry or described as actual wheel/rail force. Consumer0x62c679 passes it through additional adhesion/braking logic. Definition+0x954 is a reference pressure used in scaling, observed70 and50.

Coupling solver0x62d335 computes an endpoint force vector. Caller0x62ad6a stores a norm-derived quantity in car+0x1a0 and compares against definition+0x210, calling native uncoupler0x635dfb when exceeded. The lead read55796.06, trailing unconnected end zero, and configured threshold11000000. Full sign/end conventions and other producer branches remain to verify. No couplings were changed in this research.

Body+0xa0/+0xac are force/torque accumulator candidates; body+0x64 is the world inverse inertia tensor consumed by0x5f6368. Pointer stability is recorded, but snapshot success does not prove solver-phase consistency. On-rail, derailed, sleeping and kinematic-AI paths differ. Existing NEMT derailment modifications mean abnormal-state samples cannot be advertised as stock physics.

`read_vehicle_systems.py` preserves offset-labeled raw candidates; `vehicle-system-fields.json` adds10 meaningful candidates. Native evidence: passes24–26. Remaining: pressure propagation/release, actual forces versus requests, adhesion/wheel slip, brake types, signed coupler slack/force, broken and detached graphs, and AI physics depth.

## Brake dispatcher and pressure-event references

Pass128 verifies 276 instructions/976 bytes and pass129 verifies394/1625, all matching disk and live. Pass130 verifies914/3509 across the caller and control helper with no mismatches. These are tested-build observations, not a portable ABI.

Dispatcher005d5dec receives current car in ECX and a locomotive context in EDX. It selects brake routines using definition+910 flags and+914 type. The result combines maxima of car+19c, car+22c and zero, plus powered-car branches; steam and optional definition terms can be added. Caller005db099 stores the result at car+22c, with other branches directly scaling or clearing that field. It therefore remains a calculated brake-force request, not measured wheel/rail force. Labels for individual flags and the handbrake-like car+198/+19c fields are still provisional.

The dispatcher calls005dcdab with the locomotive context in ECX and current car in EDX. That function only proceeds when ECX equals the player lead. It compares current-car cylinder pressure+230 with its retained+268 reference. A positive difference strictly greater than1 emits event14 to player lead handles+4b6/+25c if current-car latch+26c is zero; a negative difference below-1 similarly emits event54 and uses latch+270. Each emitted direction sets its shared timer (80a1f0 rising,80a1ec falling) to1.5. Other branches decrement positive timers by player train+8e or clear the corresponding car latch. The reference updates only when absolute difference exceeds1. It is not the previous frame pressure. Timers are global, not per-car event timestamps, and may remain negative before latch clearance. Passing self as locomotive context for followers suppresses this producer; do not infer all player-car reference fields are maintained. Exact eligible call paths and timer cadence require transition tests.

Read-only capture `captures/brake-reference-paused-01/brakes.json`, produced by `read_brake_state.py`, records eight Acela vehicles at paused simulation36224.8046875, unchanged through capture, with stable ownership. All have cylinder29.0966720581 PSI and pipe95.6666641235 PSI. Powered-car force requests are74820.0078125, coaches40735.33984375; definition maximum forces180000/70000 and reference pressures70/50 respectively. Only the lead+268 matches cylinder pressure; the other seven retain zero. All observed event latches and both shared timers are zero. Constants at753c38/758b84/752d0c match disk/live as1/-1/0. This validates readable state and the distinction between pressure and reference; no active pressure transition, propagation rate or AI update is validated by this paused sample.

Helper0040698e, called by005db77b, redirects non-player-lead returns to005db7b4, bypassing the locomotive control-switch branch. It does not modify electric gate bit2. The electric sampled-gate discrepancy remains unresolved.

Next discriminating experiment: record all cars during ordinary brake release/application, correlate cylinder/pipe changes, references, event latches and shared timers; trace dispatcher-selected routine005dc4fe for the observed definition flag400hex. Preserve the separate electric-gate question and broader discovery scope.

## Acela brake adjustment series

Pass131 verifies598 instructions/2174 bytes across005dc4fe and its related export, with disk/live matching. Selected by definition+910 bit400hex,005dc4fe replenishes internal pressure stores+224/+228 from+234, computes a cylinder target using pipe+238 and definition+990/+98c, then applies rate/available-pressure branches. Its final force store is `(car230 / definition954) * definition918` when reference pressure is positive, else zero. Reservoir labels and rate/configuration token mappings remain to verify.

`read_brake_series.py` captured1740 samples with zero read errors over90 wall seconds (`brake-adjustments-01`); `analyse_brake_adjustments.py` preserves the analysis in `brake-adjustments-summary.json`. The player remained stationary with eight cars. UI semicolon twice reduced the self-lap setting (display40%,38%); four apostrophe presses returned through40%,42%,44%,46%. Raw controller+128 changed0.5 ->0.4875 ->0.475 ->0.4875 ->0.5 ->0.5125 ->0.525. This is not a full release, and UI percentages are not raw-controller percentages.

All13920 sampled car force requests exactly match the explicitly float32-rounded formula above. This observation validates this Acela path and range, not universal brake behavior or actual wheel/rail force. All sampled pipe pressures agree across cars, spanning94.8333358765..95.6666641235 PSI. Cylinder values differ across cars in six samples; lead range29.0966720581..30.7883281708, followers briefly reach31.6595325470.157 samples cross a simulation-time change during reads. Sequential copy/update logic and non-atomic capture prevent interpreting those differences as a physical propagation delay.

The lead sound reference changes29.0966720581 ->30.8137016296; its rise latch includes0 and1, rising shared timer spans0..1.25; falling latch/timer remain0. The new reference exceeds the maximum sampled lead pressure, showing an intermediate pressure state can be missed by this sampling cadence. The producer writes1.5, but no sampled timer equals1.5; do not infer a different initializer. This validates an active rising-event state transition, not audible playback, full event sequence, or falling-pressure behavior.

Final paused capture `brake-adjustments-paused-02` retains all-car state after the bounded series. Game left paused, stationary, idle, brakes applied. Next: actual release/falling event, reservoir token mapping and caller update ordering; repeat appropriate brake observations on AI. No NEMT production changes or priorities added.

## Loaded brake parameter map and reservoir state

`map_brake_parameters.py` resolves23 named tokens401a1..401b7 through the native table795790 (eight-byte entries) and parser jump table61715c. It verifies each table entry and selected branch against live memory; the first29 bytes of each parser branch also match exported analysis bytes, disk and live. Results, exact spellings, offsets and eight-car loaded values are retained in `brake-parameter-map.json`. The full parser was previously exported in pass58; pass132 independently checks the definition serialization path (1479 instructions/4417 bytes, no mismatches). No unknown runtime function was invoked.

The force reference at definition954 is explicitly `BrakeCylinderPressureForMaxBrakeBrakeForce` (token401a8). Distributor fields are:96c full-service pressure,970 emergency application pressure,974 maximum auxiliary reservoir pressure,978 emergency reservoir pressure,97c release rate,980 application rate,984 emergency application rate,988 train-pipe/auxiliary pressure difference for application,98c train-pipe control ratio,990 normal full-release pressure. Charging rates are950 auxiliary and960 emergency;958 is `BrakeAirPressureGainInResForOnePoundFromPipe`. See the JSON for all23 rather than duplicating the native spellings here.

Together with005dc4fe, these establish car224 as the distributor auxiliary-reservoir pressure and car228 as its emergency-reservoir pressure. Both refill from car234, whose exact named meaning remains unresolved. The live lead reads93.3717956543 auxiliary,110 emergency,30.7883281708 cylinder,70 at234 and94.8333358765 pipe. Definition998/99c, named `BrakeAuxilaryResPressure`/`BrakeEmergencyResPressure`, both read zero: these configuration values must not be mistaken for the current reservoir state. Loaded limits are110/110, release/application rates10/15, normal full-release pressure110, and control ratio2.0299999714. Units are pressure/pressure-rate semantics in the existing PSI model; alternate file-unit conversion remains untested.

Inventory enriches the existing configured-path entries with these runtime definition mappings instead of adding duplicate configuration candidates. Two runtime reservoir-pressure candidates are added. Mapping a declared parameter does not prove every brake branch uses it:005dc4fe's emergency branch does not visibly apply definition984 despite its name. Different brake types and AI population remain unvalidated.
