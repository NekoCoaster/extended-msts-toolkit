# Electric cab telemetry source checkpoint

Electric cab dispatcher0041f357 uses cab-view type1, while player control global7b6438 uses3 for electric. `electric-cab-fields.json` maps13 existing channel names to raw source fields, retaining reviewed branch addresses and matching assembly instructions. It covers throttle and display, acceleration high/low, reverser enum and signed display, combined-power handle/display, train/engine/dynamic brakes, main reservoir, brake pipe and pantograph. `read_electric_cab.py` gates on player type3 before reading the electric layout.

Controller=[[player train+72]+8]; lead=[player train+6a]. Electric train brake+128 and engine brake+f0 differ from steam+fc/+c4. Pantograph+264 is converted to a nonzero boolean for display; this is a controller source, not proof of roof animation, contact, line power or successful raising. DIRECTION reads an integer enum+b8; DIRECTION_DISPLAY reads float+c8. Do not replace one with the other without mapping the enum. CP_HANDLE and CPH_DISPLAY share+50; THROTTLE and THROTTLE_DISPLAY share+8c.

The guarded probe was tested against the current steam player at paused36307.70703125, correctly returning unavailable without electric-specific reads (`captures/electric-probe-steam-guard-01/electric.json`). This is a negative gate test only. Subsequent positive gameplay evidence is recorded below.

## Voltage helper and mapping correction

LINE_VOLTAGE's branch first copies [7b8d3c]+58 or divides it by1000 for cab unit0x17. When controller+258 is nonzero it calls00406cc0. That helper calls00406d2c and replaces its return address with004203ec, bypassing the apparent float comparison at004203e1. Therefore the decompiler's comparison against controller+26c is not the actual executed condition on this path. The destination is a JNZ that consumes flags preserved from the helper. The helper traverses cars via+a8, checks car+80 mask0xc and car+84 mask0x80080, and may call0040bad4. Full flag semantics and the final callee remain unresolved. Do not expose a simplified pantograph-position-equals-target formula as measured line voltage. The raw route source and effective cab display are different candidates.

The electric CAB_SWITCH enum66 produced remap175 and destination0xb7e8f44d, outside the dispatcher and image. `map_cab_channels.py` now flags destinations outside the dispatcher code span as invalid/unresolved instead of calling them identified branches. It retains the raw enum and arithmetic for research; it does not remove or prioritize that candidate, and no invalid branch was executed. Other valid branches and the68-channel inventory are preserved.

Pass102:2 functions/1114 instructions/4536 bytes, zero disk/live mismatches. Pass103:1 function/27 instructions/82 bytes, zero mismatches. These verify instruction presence only; they do not constitute an electric runtime test. Conditional brake cylinder, traction/dynamic current and voltage paths remain outside the simple13-field probe until their full conditions are represented. Next use an installed electric consist for positive reads and representative pantograph, throttle, brake and voltage transitions.

## Voltage helper completion

Pass104 traces0040bad4 (30 instructions/90 bytes; exact disk/live match). Called after the car flag checks, it loads animation-like object `[car+10]`, then its+8c value. If that value has bit0x20000000, it returns1 without dereferencing it. Otherwise it reads float `[value+4]`, subtracts2.0000000233721948e-7, and returns1 if that result is less than car+1cc or less than car+1d0 (unordered x87 comparisons also take these branches). It returns0 otherwise. The physical meaning of the object and the two bounds remains unverified.

The outer00406d2c helper walks from lead through+a8 and returns with ZF clear only after a qualifying car makes0040bad4 return nonzero; exhausting the chain returns with ZF set.00406cc0 preserves these flags while rewriting the return address. Consequently, when controller+258 is nonzero, the electric LINE_VOLTAGE display preserves route voltage if any inspected car satisfies that native condition, otherwise zeroes it. When controller+258 is zero it bypasses this check. This is a traced display rule, not independent proof of electrical contact, overhead-wire occupancy, pantograph height or physics availability. The older apparent float comparison is bypassed by the native installed helper.

## Acela positive gameplay test

Northeast Corridor / Explore Route, Acela Express trainset, Baltimore toward New Carrollton, summer/clear,10:00. This is a new session after exiting the completed steam test without saving. Current PID remains7160; train53894216, lead54492752, controller56763676 are newly discovered addresses. Five snapshots in electric-runtime-summary.json all have stable context and zero speed. All13 mapped channels were readable; constant fields are not actuator validation.

Paused baseline36013.28125: type3, throttle0, direction enum1/signed0, train-brake0.5, engine/dynamic brake0, main reservoir140, brake pipe95.666664, pantograph1. P produced on-screen PANTOGRAPH(DOWN) and raw0 at36021.5. W produced REVERSER(FORWARD), enum2 and signed1 at36029.6015625. D produced THROTTLE(3%) and raw0.025000000373 at36037.5546875. A returned throttle to0; P restored raw1 with the UP message. Final paused snapshot36057.609375 confirms idle, forward, pantograph1, speed0. Brakes remained applied throughout.

Route voltage raw stayed25000 with controller+258 gate enabled1 across all snapshots, including pantograph down. Therefore route supply and effective pantograph/traction availability must remain distinct; effective voltage was not measured by this simple source probe. The cab's numeric effort stayed0 during the observed control test, but no traction-current or effort memory producer was validated here. Reverse enum0, moving electric behavior, dynamic braking and independent electric AI remain untested.

## Live voltage-gate transition

`read_electric_voltage.py` reconstructs the reviewed native display rule using bounded external reads, never executing game code. It preserves every inspected car's flags and helper inputs, stops on the first qualifying car, and errors on cycles or a256-car cap. It distinguishes the helper's extended first comparison from its float32-rounded second comparison, with a near-boundary precision caveat. This is a derived result, not a direct read of a cab object's rendered number.

Baseline `captures/electric-voltage-up-paused-01/voltage.json` at36057.609375 finds the lead car qualifies, route voltage25000, pantograph1 and derived display voltage25000. Timed `captures/electric-voltage-transition-01/samples.jsonl` contains798 samples, zero errors/context changes,14 cross-step reads,302 paused samples. `analyse_electric_voltage.py` reproduces electric-voltage-summary.json. Route voltage remains25000 in every sample.

At sample455/time36065.30078125 the pantograph control is0 but the derived gate remains true. At sample458/time36065.45703125 the gate becomes false, requiring inspection of eight cars before exhausting the chain, and derived voltage becomes0. The0.15625-second separation is between first observed states, not exact native event latency; the50ms sampling and asynchronous reads bound precision. The raise action occurred after the timed capture ended, so its return latency was not sampled. Separate final paused capture `captures/electric-voltage-restored-paused-01/voltage.json` at36104.00390625 confirms pantograph1, qualifying lead and derived25000 again.

This distinguishes route supply, pantograph command and derived display availability. It does not establish actual current, traction power or overhead-wire contact. Three explicit inventory candidates retain the raw loaded route voltage, raw gate-enable setting and derived consist condition; the existing LINE_VOLTAGE channel retains the composed display calculation rather than duplicating it as another display candidate.
