# Physics integrator time and cadence candidates

The combined moving captures showed player position/track increments exceeding stored velocity. The physics integrator has an independent accumulator worth measuring before attributing the mismatch to velocity units or coordinate origins. This report does not establish a correction factor or claim the root cause is solved.

## Native storage and dispatch

Global80aa1c points to the shared simulation object. Existing DEFAULT-PARAMETER-FINDINGS.md establishes its constructor/default-definition provenance. Pass163 confirms005f809d selector1 writes object54(time),selector3 writes object0c(configured step). Getter005f82a5 exposes54,0c,and10(current step). Constructor005f79f5 initializes time0 and configured step approximately0.01,with current step0. Constructor00628fcb installs vtable773280 and its shared Default definition at94.

005f7fab selector2 stores solver mode at8,accepting1..10;005f9285 dispatches only modes1,3,5,7 through005fb72c,005fb8c0,005fbd4d,005fc44d respectively,and writes status20005007 on its unsupported-mode branch. Successful setters/getters write status20005000 at4;this is operation status,not overall simulator health. Object6c is the registered body count. Object84 is a zero/nonzero continuation gate in the loop.

0062924c selects mode1 or7 and a configured step bounded by its incoming delta and Default parameters788(non-derailed/all-resting branch) or790(other derailment branch). Its no-body branch directly advances the object time through the getter/setter.005f9285 starts current step10 from configured0c,forms a float target=time54+incoming delta+epsilon753e38,and loops while its local time is below that target and84 is nonzero. Solver receives start and start+current step;loop increments local time by current step and finally stores it to54. No final-remainder clamp is visible in this outer loop. The player-system scheduler00607bd0 receives the same step through explicit global7c2ac0,as established earlier.

Observed virtual slots at773280 are402ba3,4016bd,4025f9,402f5e. The latter two thunks resolve to00629d4a and005f950d. Pass164 confirms pre-step00629d4a walks body storage and sets bodyf2 bit1 for typef0==1;post-step005f950d returns immediately. Neither callback adjusts the integration step. Solver-specific behavior and actual moving accumulation still need measurement before concluding that rounding overshoot accounts for observed position/velocity differences. Do not generalize a simple ceil formula across adaptive modes or float boundary conditions.

## Read-only paused extraction

capture_motion_context.py now includes the integrator object,fields,virtual slots and ending time/step/mode/pointer rereads. It checks the known vtable and keeps the existing image SHA guard. Earlier immutable capture copies remain unchanged. captures/integrator-paused-01 has11samples,0errors,all integrator values stable,paused at day73969.7109375. Gameplay elapsed is169.7102813720703s;integrator time is270.6627502441406s. Configured/current step both0.010000000707805157s,mode1,status20005000,body count45,gate1. Pointer51424788,Default definition51739780. These are session-specific identities,not portable addresses.

This proves the two stored accumulators differ at this observation;it does not prove shared reset epochs,relative running cadence,or that the excess caused all movement discrepancies. The prior combined moving series omitted integrator time,so retrospective substitution is impossible. Future moving capture must record both together and compare positions against each without assuming either is ground truth. The capture performs only QUERY/VM_READ,no game memory writes,UI input,remote calls,or save changes.

Pass163:12functions1446instructions5229bytes,zero disk/live mismatches/errors. Pass164:2functions45instructions125bytes,zero disk/live mismatches/errors. Checks apply only to exported instruction ranges. Current NEMT timing replacements remain documented in CLOCK-FINDINGS.md;this is not stock-MSTS timing validation.

Seven candidate fields bring inventory to909. Object identity and vtable are extraction guards,not extra counts. Dynamic cadence/reset/solver-mode transitions and frame overhead remain unvalidated;this report is independent research without prioritization.

Mode1 follow-up:pass165 confirms005fb72c computes dt=end-start and updates each scalar output=input+dt*derivative after invoking the derivative callback. This is the first-order explicit update form. It does not locally shorten the passed interval;the derivative callback and alternate modes remain separate boundaries. Its own solver object differs from the simulation object. Pass1653functions320instructions959bytes match disk/live without errors. This supports testing fixed-step accumulation as a hypothesis,not claiming runtime overshoot or complete callback behavior from static code.

## Moving player and AI comparison

Reloaded Grain Train Through the Night through the normal UI without saving. Started the bounded130wall-second sampler during briefing,closed briefing,then applied Backspace penalty/emergency braking early. Player slowed to zero while AI approached. integrator-moving-grain-01 preserves1296samples,0errors,98initial paused samples,day73800.6875..73921.015625,gameplay elapsed0.683653593..121.018531799 and physics time0.720653832..195.234848022. Mode remains1;configured step range0.008354600..0.010000001s. A lower physics time after reload supports reset/reinitialization between activities,but does not establish the exact reset instruction/order or epoch from the first post-load sample.

analyse_integrator_motion.py compares identical eligible adjacent car pairs under both clock denominators. It requires positive gameplay delta<=1s and physics delta<=2s,both unpaused,stable integrator pointer,car/body/definition/link/owner checks,unchanged origin,node,direction,not derailed,and mean body velocity norm>0.5m/s. Source SHA is retained. Same-clock subset requires both gameplay elapsed and integrator time unchanged within each endpoint read. Reads remain sequential:initial gameplay clock precedes train reads,initial integrator fields follow the base train read,and detailed track checks follow them. This is not atomic acquisition. Neither series is declared ground truth,and no outliers are clipped.

Across1197eligible sample intervals,physics/gameplay elapsed ratio median1.616827,p95=1.816198,range1.114498..2.038469. This is the ratio observed in this installed configuration/run,not a universal multiplier or proof of each internal frame's step count.

Player400007:1802eligible car pairs,814same-clock. Median absolute body displacement-speed versus stored velocity discrepancy falls from5.179556m/s using gameplay elapsed to0.002479m/s using physics elapsed. Corresponding p95 values7.554567 and0.531213;max10.266155 and1.362031. Track-distance median discrepancy falls from5.157436 to0.008972m/s,p95 from7.551736 to0.029485. Same-clock body median falls5.115864->0.002382,p95=7.312402->0.534837,max=7.608468->1.295778. This strongly supports the independent physics time base as an explanation for the large typical player discrepancy in this run. Remaining outliers and asynchronous placement prevent exact-equivalence or universal accuracy claims.

AI400005:1790eligible pairs,285same-clock. Body discrepancy median0.664115m/s using gameplay elapsed becomes8.205889 using physics elapsed;track median0.755880 becomes8.626316. Same-clock body median0.303130 becomes8.629415. Thus substituting shared physics elapsed for physical AI is inconsistent with these observations. Earlier service-update traces document its separate service timing path. AI residuals under gameplay time still need phase/cadence investigation;these results do not validate every AI velocity sample or eliminate previously captured reset-phase zeros.

The prior wall-clock substitution result remains correct:wall time alone did not solve the player discrepancy. The newly captured internal physics accumulator changes the interpretation. Store time-base identity alongside sampled/derived motion;do not silently rescale stored velocity,replace gameplay time for all entities,or use measured ratio as a constant. The previously traced full-step loop is a plausible source of accumulation differences,but exact per-frame execution and loaded timing modifications still require separate attribution. No timing/game settings were changed.

After capture completion and an unsampled tail,the game was paused. integrator-moving-final-paused-01 records73934.734375,gameplay elapsed134.731185913,physics217.280517578,player23cars speed0,AI22cars stored speed21.130149841,both0derailed. All capture/analysis jobs ended. Inventory remains909;this improves interpretation and validation of existing candidates without adding redundant signals. Next re-evaluate connection derivatives with appropriate time bases and trace/measure frame-step accumulation;broader station/infrastructure/lifecycle research remains open.

## Connection-rate comparison under both clocks

Reanalysed the same immutable integrator-moving-grain-01 capture with explicit gameplay and physics denominators. analyse_connection_motion.py now requires the same positive bounded deltas and stable integrator identity for either choice when integrator data is present. It does not automatically choose a time base by train type. Both outputs preserve identical source SHA and pair counts. Same-clock checks both elapsed and physics clocks at each sample endpoint;it is still not atomic acquisition.

Player:1692eligible comparisons,908cross-clock. Absolute difference between finite gap rate and mean projected stored-state relative velocity has median0.004428m/s using gameplay time versus0.000740using physics time;p95=0.034988 versus0.009548,max1.181058 versus0.678201. Same-clock median0.004792 versus0.000696,p95=0.031077 versus0.008915,max1.002067 versus0.650820. Physics time improves typical agreement but does not remove the substantial outliers. The worst physics pair59266312/59265384 changes sampled gap0.092994786->0.203023815m over73800.984375..73801.0859375,finite rate0.687690 versus projected0.009489m/s. No physical stretch/compression claim follows from that asynchronous gap jump.

AI:1649eligible comparisons,1376cross-clock. Gameplay absolute rate discrepancy median0.001687,p95=0.995968,max6.556522m/s;physics median0.001356,p95=0.803339,max4.259201. Same-clock gameplay median0.001348,p95=0.215696,max6.556522;physics median0.001166,p95=0.215785,max4.259201. AI worst pair68764676/68779556 at73888.5703125..73888.671875 has sampled gap0.807957133->0.212216661m and projectedrate+0.533776m/s;finite gap rate is-6.022745(gameplay)or-3.725424(physics),with unchanged clocks during both reads. Same-clock does not establish coherent producer-phase state.

A numerically smaller AI relative-gap discrepancy under a longer time denominator is not evidence physics time governs AI. Most relative rates are near zero,and dividing an apparent gap jump by more time can reduce discrepancy mechanically. The independent absolute body/track comparison above strongly worsens with physics time for AI. Do not select a clock by minimizing this relative statistic alone. Clock choice,zero stored angular velocity,placement/reset phases and asynchronous sampling remain distinct limitations. These results qualify existing connection candidates,not a new signed-slack/force signal.
