# Stored motion versus observed position/orientation changes

`analyse_motion_differences.py` reuses three immutable vehicle-motion captures and records their SHA256s in `motion-difference-summary.json`. It compares adjacent successful observations only, with0<simulation delta<=1second, both unpaused, neither car derailed, successful body-pointer checks at both ends, and unchanged train address/car identity/definition/links. Body buffer addresses may legitimately swap. It resets continuity across error rows. No outlier clipping or origin correction is applied. These are observational consistency checks, not a reference physics implementation.

For each eligible car pair it compares `(position1-position0)/dt` with mean body88 velocity, and `(forward1-forward0)/dt` with `mean(body94 angular velocity) cross mean(forward)`. Results report vector-norm discrepancies, separate same-clock subsets, and the worst examples. Same-clock means each endpoint read saw an unchanged simulation clock; it does not establish an atomic read, common producer phase or constant coordinate origin. Threshold1e-6/s is used only to count appreciable forward-vector changes; both endpoint angular velocities must be exactly zero for that count.

In vehicle-motion-ai-01,AI400005 has4673eligible car pairs. Mean stored speed ranges14.9552..24.8107m/s. Position-derived versus stored velocity discrepancy has median0.001658m/s,p95=0.661379,max1.007648. The same-clock subset has median0.000404,p95=0.155863,max0.980932. Stored angular velocity is zero throughout all eligible comparisons, but3875pairs have forward-vector change above the counting threshold, including2266same-clock pairs. Maximum forward derivative norm is0.150531/s (same-clock max0.149132/s). These are correlated car observations,not independent events or elapsed durations.

In vehicle-motion-restart-01,player400007 has4484eligible pairs. Position-derived versus stored velocity discrepancy is much larger:median3.862139m/s,p95=6.918741,max8.572567. Same-clock median3.857089 and max8.572567 show that simply discarding clock-crossing reads does not resolve it. Worst example car59306424,73800.8359375->73801.0390625,dt0.203125:position-derived vector(15.1030649,0.3112981,-12.8449519),mean stored vector(8.5736923,0.1769111,-7.2917428). No cause is assigned from these samples alone. Origin movement,producer cadence/integration paths and existing runtime modifications need investigation before converting these differences into a correction factor.

All eligible player-restart angular velocities are also zero;2881pairs show changed forward vectors,2339of them same-clock. Maximum derivative norm0.021325/s. The earlier stationary player observations have only tiny velocity residuals and no forward changes;their agreement cannot validate moving behavior.

This strengthens a practical limit: body94 is a stored angular state,not a universally valid derivative of track-following orientation. Native005e17a0 endpoint velocity is calculated from stored body88/94,so reconstructing that formula does not establish actual endpoint trajectory velocity for track-following player or AI. Positive/negative projected relative velocity retains its algebraic convention,but actual opening/closing must be validated against endpoint positions over time. These captures omit definition400/414,preventing honest reconstruction of the exact solver endpoints. No endpoint/slack validation is claimed.

No source captures,game memory,assets or UI state changed. Candidate count stays902. The new evidence qualifies existing body/derived velocity entries rather than inventing a new signal. Next: capture body/definition/track state together during controlled movement,compare world and track-coordinate increments and producer timing,then validate connection derivatives. Avoid treating a position finite difference or stored velocity as ground truth by default.

## Time-base check and AI angular-state reset path

The analysis now also uses each archived row's monotonic wall timestamp. For eligible player-restart pairs,simulation/wall interval ratio median1.013546,range0.897997..1.169124. Using wall intervals instead of simulation intervals leaves linear discrepancy median3.930418m/s,p95=6.878595,max8.835120. The worst simulation-time example has wall delta0.200470600s versus simulation delta0.203125s. A simple substitution of wall time does not explain the large player discrepancy. This does not exclude frame integration,clock patch effects or sampling phase;wall time is not automatically the correct physics time base. Ratios are car-pair-weighted,not independent frame statistics.

AI interval ratio median0.999245,range0.967769..1.030581. AI wall-based discrepancy median0.032831m/s,p95=0.408232,max0.987556. Differences between these summaries and simulation-time summaries remain descriptive. Existing CLOCK-FINDINGS.md documents NEMT timing replacements;no stock-timing claim or setting change is made.

Pass162 resolves two important helpers.00636475 invokes00628d09 with ECX=car body and stack argument=car after its track placement/copy path.00628d09 checks definition flags8c bits80/40,sets body metadata,places it via0063484b or00634b00,and on success copies the vector at83db48 into body58 angular momentum,body94 angular velocity,and other velocity/force stores. `angular-reset-provenance.json` confirms that current reset vector is(0,0,0);it also records unchanged image hash and paused28919.865234375.

AI service updater005a7337 then sets linear velocity/momentum and world inverse inertia. At005a752a it passes body58 as the vector and at005a7533 body64 as the matrix to005fd74f;the result is stored into body94.005fd74f is the explicit three-row matrix/vector multiplication. Consequently this AI update can place/reorient the car along the track while deriving angular velocity from cleared angular momentum,not from the change in track orientation. This is a concrete native path consistent with the observed zero angular states;not proof of every alternate caller or execution frequency.

All9944archived AI car observations have zero angular momentum and angular velocity. The accompanying stationary player11040observations and restart player8050observations also have zeros. These counts include paused and unstable observations;the earlier eligible-pair subset remains the basis for motion comparison. The AI path does not by itself establish the player position-discrepancy cause. Pass16210functions1829instructions7111bytes match disk/live. No UI input or game write occurred.

## Combined moving body/track capture

`capture_motion_context.py` records body geometry/velocities,definition400/414 and spring bytes,per-car/service track records,origin tile,both simulation clocks,frame delta,time scale,train accumulators and beginning/end monotonic times. Each new capture copies its probe dependencies and records hashes. Memory access remains query/read only. The21sample Maryland baseline has0errors,unchanged28919.865234375 and maximum read duration0.003151s;it checks extraction only.

Through the ordinary UI,exited Maryland without saving and selected Marias Pass / Grain Train Through the Night. Started `motion-context-grain-01` while the briefing was open,closed it,and pressed Backspace early to apply the penalty/emergency brake. The train visibly slowed from24mph through18mph to0. No save or memory write.748samples over75wall seconds have0reader errors,98initial paused samples,simulation73800.6796875..73865.9296875,and maximum read duration0.008616s. Origin tile remains(-12560,14766). Only the23-car player400007 was physically recorded during this series.

`analyse_motion_context.py` uses elapsed-clock80acd0 differences,stable body/definition/link/owner checks,unchanged origin/node/direction,both endpoints unpaused and not derailed,and mean stored speed>0.5m/s.2162eligible adjacent car pairs include1209clock-crossing pairs. Signed discrepancies (unsigned average displacement speed minus mean stored speed magnitude) have body median+5.349602m/s and track median+5.354168m/s. Body-minus-track median+0.000858m/s,p95+0.057097,range-2.341374..+2.257371. These are descriptive car-pair distributions,not independent events or accuracy guarantees;short windows and asynchronous reads account for unassigned outliers.

Both movement measures exceeding stored velocity reproduce the earlier concern with the missing inputs now present. Consistent origin tiles and track-distance increments weaken a simple origin-tile-shift explanation for this run. They do not identify the responsible integration/track-placement caller or rule out every coordinate/phase effect. Worst body discrepancy occurs in a same-clock pair:time73801.765625..73801.8671875,elapsed delta0.098138s,body20.920440m/s,track18.697767m/s,stored10.847080m/s. No correction factor is justified.

After the bounded sampler completed,the game was paused. `motion-context-grain-final-paused-01` reads73885.8046875,player40000723cars speed0,AI40000522cars stored train speed15.41961956,both with0derailed flags. The gap between series end and pause is not captured as a continuous motion series. All new addresses must be rediscovered after this reload. Next: resume a bounded combined AI observation while player stays braked;derive complete endpoint trajectories from this new definition-inclusive capture and investigate the player track-distance producer. Maryland station tests remain open independently.

## Combined AI movement and connection derivatives

`motion-context-ai-01` contains649samples over65wall seconds with0errors,210initial paused and439unpaused samples,simulation73885.8046875..73929.921875. Maximum read duration0.014646s;origin tile remains(-12560,14766). Player400007 stays stopped while22-car AI400005 passes. Ordinary Escape inputs resume/pause;no game-memory writes or save overwrite. Final separate paused capture reads73969.7109375,player23cars speed0,AI22cars stored speed16.209196m/s,all0derailed flags. The interval after series completion is unsampled.

AI combined analysis finds1880eligible moving car pairs,1639cross-clock. Signed body displacement-speed minus stored-speed discrepancy median-0.053160m/s,p95+3.731025,max+15.127529;track-minus-stored median-0.068462,p95+3.759240,max+15.129730. Body-minus-track median-0.001465m/s. These distributions include phase-sensitive outliers;the stationary player has no eligible moving pairs in this series.

`analyse_connection_motion.py` reconstructs complete solver endpoints and stored-state endpoint velocities,then compares adjacent gap differences over elapsed-clock time with mean projected relative stored velocity. Filters require unpaused positive delta<=1s,stable reciprocal links/owner/definition/body checks,unchanged origin,not derailed,and train speed magnitude>0.5m/s at either endpoint. These correlated pair counts are not independent physical events. Same-clock means unchanged dayclock within each read,not atomic acquisition. No outliers are clipped;double-precision reconstruction and approximate1e-7epsilon are not bit-exact native arithmetic.

Player:2021eligible comparisons,1104cross-clock. Sampled gap range0.060640..0.400992m. Absolute rate discrepancy median0.003163m/s,p95=0.046573,max1.150684. Same-clock median0.003438,p95=0.050132,max1.128129. Worst pair59262860/59281412 at73801.2578125..73801.3515625 has gap0.2000098->0.09294526m,finite-difference rate-1.149245m/s versus stored-state mean+0.001439m/s. Apparent jumps are not proven physical compression.

AI:1736eligible comparisons,1505cross-clock. Sampled gap range0.005261..0.488253m. Absolute rate discrepancy median0.001095m/s,p95=0.544040,max11.368943. Same-clock median0.000781,p95=0.113682,max0.481259. Worst pair68256672/68316576 at73902.4921875..73902.5859375 has gap0.201573793->0.201575709m,finite-difference rate+0.000020416m/s versus stored-state mean-11.368923m/s.

That AI outlier coincides with car68256672 body velocity changing from(-15.7872305,0,16.2811050) to(0,0,0),while its scalar car velocity changes22.6784267->22.7410030m/s and train speed22.6784306->22.7409992m/s. Following car68316576 retains nonzero body velocity at both endpoints. Both body pointers pass after-read stability checks,although both rows cross the dayclock. This is consistent with observing an intermediate reset/refresh phase in the previously traced AI placement path;it is not an execution trace or evidence the AI physically stopped. Exact samples remain in the immutable source capture.

The four connection candidates are now evaluated during moving player and AI observations. Formula reconstruction is supported;universal equivalence to actual endpoint trajectory rate is not. Neither stored-state projection nor asynchronous finite differences establish force,signed slack,physical compression,or a correction factor. Next investigate producer phases and player track-distance integration;do not infer consistency from near-zero medians alone. Inventory stays902;no selection or prioritization.
