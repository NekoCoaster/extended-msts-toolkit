# Junction ownership and vector service lists

A new paused snapshot at73934.734375 distinguishes more infrastructure state from selected branch,signal aspect and abstract per-car presence. Query/read access only;no switch operation,UI input,save change,process write or remote native call.

## Native release semantics

Pass166 confirms signal release005c4ecf checks signal24 against the supplied service before clearing it and flags1c bits0xe000. A different route cleanup005959d0 handles junction58 via00595b41,vector38 via00595b6e,and signal24 separately. This does not establish equivalent reservation meanings across those fields.

Pass167:00595b41 clears junction58 only when equal to its supplied service,then invokes005d0fa6.00595e76 visits every registered node,using this matching-owner release for kind2 and vector-list removal for kind1.005c1d7d also releases along traversed route segments and stores a junction reference in service178;its exact advance/release boundary remains to trace.

00595b6e removes the supplied value from the list at vector38. If the circular sentinel becomes empty,it frees that list,clears38,and sets byte35 to3. Pass170006900b0 confirms nodes contain next0,previous4,value8;it searches by value,unlinks one matching node,and recycles/frees the list entry. A null list is therefore a possible lifecycle state,not necessarily unknown memory. Acquisition,duplicate handling and exact meaning of byte35 remain unproven. These lists differ from vector2c per-car position records.

Pass168/169:005d0fa6 restores junction50 from005d0f1e on one-input/multiple-output or multiple-input/one-output junctions. Assembly explicitly passes side1 or0 respectively.005d0f1e uses junction54 as a shape index into [[80a118]+4]+index*0x14;the multi-pin side returns shape byte+0xa,while the single-pin side returns0. Thus release can restore a shape-defined branch;this is not proof of immediate blade animation,universal straight/diverging numbering or permission to travel.005d1003 also invokes this restoration across all junctions independently of matching-owner release.

005959d0 clears junction4e bit2 during its traversal. No full bitfield meaning follows from that mask. Read it as uint16 raw flags pending writers/consumers. Signal helper005c14b9 turned out to evaluate/update aspect and drawing bytes,not acquire service24. The association setter has therefore not been located in this pass;no stronger reservation label is assigned.

## Live snapshot

read_infrastructure_owners.py reuses the bounded full database registry,service registry and topology enumeration,then reads junction58/4e and vector38/35. It records script hashes/copies and beginning/end pause/clock values. captures/infrastructure-owners-paused-01/owners.json reads246junctions408vectors,unchanged clock,all owner/list-pointer rereads stable.

Three nonnull junction associations join the player service0:routeIDs249(branch0,flags0),284(branch0,flags1),302(branch1,flags0). The other243junctions have null association. Five vectors have nonempty service lists:305(player0,byte35=1),292/295/300(player0,byte35=0),310(AIservice2,byte35=0). All five stored values join the service registry. The native cleanup's service parameter and these joins support calling them service associations. This is a snapshot,not observation of actual acquisition/release transitions.

Four new flat candidates are junction service association,junction raw flags4e,vector service associations,and vector raw byte35. They do not duplicate the existing selected branch or vector2c per-car presence fields. Associations do not by themselves prove occupied track,whole-train extents,reservation exclusivity,AI physical instantiation or movement authority. Null does not imply clear/safe. Pointer/table IDs remain route-build/session-specific;non-atomic list reads can race and identity checks cannot exclude reuse.

Pass1666functions296instructions895bytes;pass16711/634/1962;pass1684/79/223;pass1698/1180/3599;all disk/live exact. Pass170generic list helper expanded to116callers/functions(13640instructions47911bytes),all disk/live exact;only006900b0 and the relevant infrastructure caller were used for semantic conclusions. Avoid another broad generic-container export;future work should target acquisition or typed call sites. No inference comes merely from unrelated exported functions.

Next locate typed setters/acquisition paths,then record the oncoming AI's transitions against selected branches,signal aspects,service lists and per-car presence together. The full research goal remains open. Inventory913;no ranking or retention decisions.

## Acquisition paths and directional claim state

A bounded Ghidra instruction scan over00590000..005d2000 located MOV destinations at offsets58,38,35,24. It is a candidate-site search,not object typing or exhaustive writer coverage. The preceding4097-byte neighborhood rangepass171 found release sites but no acquisition;its arbitrary range decode was only navigation evidence. Typed function exports172/173 are the basis for the following semantics. No functions were invoked in the game.

0059702a walks a candidate route in two passes. During its first pass,a non-player service is rejected by junction4e bit2;an existing junction58 owner must match the supplied service and junction50 must match the requested branch,otherwise traversal stops. The second pass writes the supplied service into58 and calls005976e2 with the branch.005976e2 updates50 and invokes005c1a06 only when that selection changes. This supports a route-claim/branch-conflict role,not a physical occupancy flag. Overall return meaning,rollback,all alternate acquisition paths and dynamic authorization remain to validate.

A separate00598476 writer can also store service into a junction58,with an accompanying value in5c. In other branches it writes signal28 or kind9item1c,not signal24. These are distinct association fields and must not be conflated. Their broader semantics are not yet inventoried as established meanings.

0059867c takes a service,vector node,related-node parameter,requested code and commit flag. It compares vector34 (constraint/state input) and35(current claim state). Request0/1 can match34 directly or follow the special34=2/3 branches. Current35=2 permits only the first listed service;opposing0/1 requests can yield state2 when all existing list values equal the requesting service. Request2 is accepted through34=3 under the listed conditions. Other combinations return0. A nonzero commit flag writes35 and may allocate/append the service to38;zero performs the check without those writes. A related-node/membership branch avoids some insertions,so counts cannot be treated as unique service/train counts.0059888b returns the common service only if every stored value equals it;duplicates of one service do not invalidate that condition. Requests0/1 are corroborated as direction-related by005988e9 passing pin direction bytes,while2/3 retain contextual rather than universal enum names.

The commit writes35 before list allocation/insertion;on insertion failure the helper can return0 after that write and clears an empty list. Therefore success/failure and all sampled fields need not form an atomic transaction. Do not recreate claims or call this helper from telemetry code.

Signal005c4e55 supplies the previously missing service24 acquisition rule. Nonnull24 equal to requester returns1;different owner returns0. With null24 and flag1c bit0x8000 clear,it sets the bit and stores the requester. With null24 and that bit already set,it returns1 without storing requester. Thus successful return does not always establish ownership.005c199b applies the helper across matching-direction kind0items and discards individual returns. These are native branch semantics,not evidence that the exceptional null/flag state occurred in gameplay.

Fresh infrastructure-acquisition-paused-01 retains73934.734375 and the same3junction/5vector associations,all rereadsstable. Vector34 distribution:392value4,8value0,7value1,1value3. Vector35:403value3,4value0,1value1. The five nonempty lists have34/35 pairs:route305=1/1,310=0/0,292=0/0,295=0/0,300=3/0. The prevalence of34=4 needs its producer traced;do not call it occupied/closed. All292signals were read,5associated;none hadnull24withbit8000set. No live claim transition is claimed.

One additional candidate,vector raw constraint/state byte34,brings inventory914. Existing claim candidates now have acquisition evidence but retain dynamic/exclusivity/occupancy limits. Pass1729functions1625instructions6090bytes andpass1737functions712instructions2467bytes match disk/live exactly. The game remained paused,noUIinput,game-memorywrites,saveorasset changes. Next capture typed claim/aspect/presence transitions around the AI encounter,then trace34producer and distinguish signal24from28.

## Live within-node observation and presence disagreement

capture_infrastructure_transitions.py traverses the complete route registry each sample and pairs junction branch/owner/flags,vector34/35/service lists,signal aspect/flags/service24,and per-car presence records with service/physical-train identities. Captures preserve reader dependencies and hashes. Full acquisition is sequential;observed maximum read duration0.140271s is a read-window measurement,not a measured game-performance overhead. List races may produce retained errors;stable pointers do not prove coherent contents.

Normal UI activation/resume/pause only;player remains braked. infrastructure-ai-transitions-01 records241samples over120wallseconds,0errors,20initialpaused,day73934.734375..74045.2421875. Registry node map stays unchanged. No sampled junction,vectorclaim,signalaspect/flags/association orpresence membership/count changes occur. This is negative transition coverage,not successful acquisition/release validation. Initial/latest AIstoredspeed21.130150->15.646396m/s,22cars;player23cars0speed,both0derailed. The sampler ended before final pause;that tail is not part of the continuous series.

Presence distances do change:AIservice2 remains onroute310with22records,range11181.347656..11569.250977m atseriesstart and9339.310547..9727.213867m atlastsample,a1842.037109m shift. Player remains on305with23records andfixed10446.100586..10883.472656m. The analyzer records continuous distance ranges separately from discrete transition counts;an empty event list does not mean no movement. This run stayed within the same vector nodes. A later paused track read reportsAI node310length11887.432617m,so the run did not reach its decreasing-distance endpoint. Another identical-length run is not justified solely to seek events;choose a measured boundary or trace updates first.

Final paused74054.6171875 captures infrastructure-transition-final-paused-01 and infrastructure-transition-track-check-01 have identical beginning/end clocks. check_presence_track_snapshot.py compares sorted(node,distance)multisets per service:player23matchesexactly;AI22matchesnodes/count but everypresence distance is+9.65234375m versus physical-car track distance. This is not a per-car identity join and does not prove physical occupancy extent. Constant offset is compatible with timing/reference differences;its producer/cadence cause is unresolved. Preserve the two surfaces separately rather than substituting one for the other. Paused clock equality alone does not remove stored-state age differences.

No new field count:inventory914. Runtime evidence now establishes moving AI presence distances,with a concrete player/AI equivalence boundary. Acquisition/release,branchchanges andsignaltransitions remain open for these typedfields. Next trace per-car presence producer and update cadence,then select a reachable infrastructure boundary from measured track position;do not infer transitions from the AI's speed or visual proximity alone.

## AI extrapolation explains the paused gap numerically

Pass174 verifies 13 exported functions,2799 instructions,11486 bytes against disk and the live process with no mismatches or read errors. Reused scheduler005ae731 computes age from day clock minus service150 (with a day-wrap branch), then calls005a5c9a for physical AI. The latter copies service4c to a local track record, computes travel using absolute speed138,acceleration140,target13c and age, advances that local record, and passes it to005a7337 for physical-car placement. It does not overwrite stored service4c with the extrapolated track record. This is a concrete reason stored service state and physical-car placement can have different ages.

check_presence_track_snapshot.py now reconstructs that formula from the immutable paused pair. Day74054.6171875 minus service150=74054 gives age0.6171875s. Speed15.64639949798584m/s,acceleration-0.021407032385468483m/s² and target15.6298246383667m/s select the branch before reaching target. Predicted terminal speed15.633187345185434m/s and trapezoidal travel9.652685002384885m match the absolute9.65234375m presence/physical gap across all22 AI distances with maximum residual0.000341252384885m. Constant-speed travel alone is9.656762190163136m. Player23 distances remain exactly equal.

This supports a timing explanation for this sample; it is not yet a verified presence-record producer chain. The comparison uses sorted multisets, Python double arithmetic rather than exact native x87/float rounding, one same-day paused observation, and no cross-node identity proof. Do not apply a universal9.65m offset or regard a paused process as guaranteeing all stored surfaces have equal age. No new candidate count or production change.

## Presence producer chain resolved

The subsequent pass177/pass178 trace resolves the missing producer. 005a5c32 takes ECX=presence record and EDX=track record. If the track node changes, it inserts through005d0464 when the old node is null, otherwise moves the existing list entry through005d0334. It then copies track+0 to presence+0 and track direction byte+c to presence+18. Regardless of node change it copies track distance+10 to presence+4. Direction refresh is conditional on node change in this helper; do not assume it is continuously refreshed during an in-node reversal. Helper insertion/move return values are not tested before copying record fields, so failure paths need separate treatment.

005a41c2 copies service4c to a local0x68-byte track record and passes it to005a4f86. Subject to service144/148 gates,005a4f86 walks service12c;005a5b44 walks backward through vehicle spacing from that track record and calls005a5c32 for each payload. Thus the AI abstract list is updated from stored service track state, while005a5c9a separately extrapolates physical placement. This chain and the numerical paused comparison together support the different-update-age explanation. No sampled call tracing or broad lifecycle proof is claimed.

An alternative producer005a5167 walks its supplied presence list and passes record+10 physical-car pointer plus128 to005a5c32. Caller00587c17 supplies lists at object1ac and2f4. The outer caller's cadence and complete list ownership remain to be traced; do not assert all AI records follow this physical path every frame. 005a7821 also initializes records;005a8f33 reinserts an existing non-null record. Moving005d0334 splices the existing list node between track-node2c lists and clears an empty old list; it may reuse the old sentinel. Native list contents are not safe lifetime handles.

Pass177:7functions466instructions1421bytes;pass178:15functions1945instructions7845bytes;all exported instructions match installed disk and live memory with0errors. Pass175/176 ranges and presence-producer/insertion reference exports are navigation evidence only. The first insertion-reference query also included005d033d,which returned no references;the actual function boundary005d0334 was identified and exported inpass178. Inventory remains914. Next test repeated update ages/per-car identity and an actual node crossing;signal/claim transitions remain unvalidated.

Follow-up pass179 identifies outer caller0062924c: after005f9285 returns from physics integration,00629956 sets ECX=00809810 and calls00587c17. Thus the two physical-refresh lists are rooted at008099bc and00809b04. This call is in the nonzero-body-count branch and subject to the outer incoming-delta bound at00753fb0. It is once after the integrator call on that branch,not necessarily once per solver substep. This establishes ordering relative to physics integration but not complete list ownership or ordering against the separate AI scheduler. Pass179:1function735instructions2814bytes,disk/live exact with0errors. No game input or memory write was used. The prior turn is concrete progress;goal remains active and incomplete.

## Paused individual-car joins

read_presence_identity.py captures all45 presence records at paused74054.6171875,joining record10 only to independently enumerated physical-car addresses before dereferencing. Every join is distinct and every car258 backlink equals its presence record;all service joins,node comparisons and record rereads match. The23 player records have0distance difference and direction1/1. The22 AI records each have presence-minus-physical9.65234375m and direction0/0. This validates the earlier distance comparison by individual-car pointer/backlink identity at this state,not merely by sorted multisets.

The list rooted at008099bc contains exactly the23 player records;00809b04 is empty. None of the22AI records is in either post-physics refresh list. Combined with the traced stored-service writer and extrapolator,this explains why this paused physical AI is not refreshed through the player list. Other consists,detached vehicles,initialization/teardown,reversal and scheduler phases remain unvalidated. Pointer identities are session-local and may be reused. New capture is read-only;PID7160 remains paused,with no UI action or game asset change.

presence-identity-summary.json and analyse_presence_identity.py preserve reproducible identity/count/direction/distance checks and the capture hash. New flat candidates expose record10 physical-car association,car258 presence backlink,and record18 direction byte,with conditional-update and lifecycle limits. The raw record14 word is retained in the capture as a candidate index,not promoted to a stable vehicle identity.

## Moving identities and physical AI removal

presence-identity-moving-01 captures261 samples at0.25s target intervals over65wallseconds,0reader errors;57initial samples are paused. Recorded day range74054.6171875..74105.6171875;maximum sequential read duration0.1159698s. Normal UI resume/pause only. Final separate paused capture presence-identity-final-paused-01 is at74119.125 after an unsampled tail.

Samples0..96 have45 enumerated physical cars. Sample96 spans74064.453125..74064.5390625 with22joined AI records. Sample97 spans74064.703125..74064.8046875:physical registry falls to23player cars,AI service2 train158 becomes0 and physicalized134 becomes0,and all22presence record10 physical pointers become0. The same22presence addresses,configuration references,service and raw index14 remain,with stable records and the same track node. They continue moving:all22stored distances decrease641.505859375m between sample97 and last sample260. No track-node crossing occurs. Thus loss of physical cars is not service deletion or abstract-presence removal. Exact transition instant,trigger distance and rephysicalization remain unproven.

analyse_presence_motion.py records22pointer-association changes (to null),rather than hiding these as failed joins. The3608unjoined record observations after removal are retained and excluded from physical-position comparisons. Before removal,40moving samples provide880correlated per-car comparisons across10distinct AI update timestamps. Observed update ages0.6796875..1.453125s show that age need not stay below1s;the analyzer uses a conservative same-day0..60s guard and does not discard the1..1.453s samples. No midnight assertion.

Start-clock reconstructed gap error median1.588824689m,max1.831641712m;end-clock median0.000267960m,max0.244449071m. Physical gaps vary12.2236328125..24.0810546875m. None of these40moving samples has identical beginning/end clock. The strong improvement with end clock is consistent with cars being read late in a roughly0.1s route scan;remaining outliers and internal update phase are unresolved. Do not retrofit a universal time offset or claim per-car atomicity. Paused1254correlated comparisons across57samples retain0.000341252m error. This is repeated timing evidence plus one physical-removal transition,not broad lifecycle or cross-node validation.

## Restarted approach: acquisition transitions and failed crossing attempt

The boundary-planning-paused-01 track capture places player leading car at10883.47265625 of11946.1689453125m on route node305,leaving1062.6962890625m to its increasing-distance end. boundary-topology-paused-01 verifies endpoint junction302 links to vectors300,310,305. Addresses from that run must not be reused after reload.

The activity was exited and the already-selected Grain Train Through the Night restarted through normal UI. New player start at25mph/notch2 was restored. After speed fell on the grade,normal throttle keys increased to notch3 then4. infrastructure-player-approach-01 sampled all route nodes at0.5s target interval. The player passed a red signal before the node boundary,and MSTS displayed 'Activity failed -- ignored red light.' This is a failed driving/crossing experiment;the monitoring interval was too long for this approach. No successful red-to-clear wait or node crossing is claimed. Existing installation settings previously recorded as IgnoreRedSignal/PreventActivityEnd do not establish that this restarted run suppresses failure. No setting was changed.

Sampler was intentionally interrupted after the alert rather than waiting through the remaining requested240s. Its flushed file contains300complete samples,0read errors,119paused samples andday73800.6875..73891.171875;maximumread0.1405904s. Exitcode1 is intentional interruption,not a normal completion report. Game remains at the failure alert. Player has23cars and0derailed flags;AIphysical train appears during capture with22cars and0derailed flags. No presence node changes occur:playerstays305,AIstays300. Player front presence reaches11852.02734375m,about94.14m short of305end.

Despite failing the crossing objective,the capture supplies first typed live acquisition transitions. Between73800.6875 and73800.9765625,junction302 owner58 changesnull->AIservice2 whilebranchstays0;vector310 state35 changes3->0 andlist38 gainsAI2;vector305 changes3->1 andgainsplayer0;vector300 changes3->1 andgainsAI2. Constraint34 remainsunchanged. These initialization/resume brackets include a paused read;they are not all steady-state moving transitions.

Between73800.859375 and73801.46875,signalitems336,306,301 gainAI2association24 andflags change48->49200,1->32769,1->32769 respectively. Their aspects are4->4,0->0,0->7. Item300laterchangesaspect2->3 without association/flags change,bracket73804.359375..73804.96875. Itemindices are current route database indices,not universal signal IDs or proven identification of the red signal passed. Association changes can occur without aspect changes,and aspect changes without association changes. Selectedbranch did not change during this series. No release transition captured.

infrastructure-player-approach-01-summary.json preserves all8events andsourcehash. Unchanged pointer checks do not make reads atomic;all event pairs cross at least one simulation-clock step. Next restart,stop well before node305distance11852 and wait for oncoming AI clearance before attempting crossing. Retain failure evidence and avoid relying on the prior settings snapshot as a runtime exemption.


Follow-up before the next restart: dismissing the red-failure alert allowed gameplay to continue briefly;the activity was then paused and exited normally. The evaluation screen reported exit after1minute39seconds and one speed-limit episode totaling7seconds. Therefore the alert is not proof that the simulation was irreversibly terminated. Preserve the observed failure alert separately from continuing-gameplay/exit behavior;no installation setting was changed. The prior crossing attempt remains invalid as normal signal-observant driving.

## Successful stationary-player AI crossing and route release

Restarted Grain through the evaluation screen,closed briefing,and immediately applied the normal emergency-brake key to stop well before the signal. No switch operation,game-memory write,asset change or save. infrastructure-ai-clearance-01 completes normally:421samples,0errors,43initialpaused,day73800.6796875..73989.90625,maxread0.161478s,route map unchanged. Player starts23cars11.002593m/s and ends23cars0m/s;AIends22physicalcars15.646396m/s,both0derailed. Player remains on305,front distance ends10973.1484375,about879m before the previous red-signal failure position. Final separate paused snapshot is73997.5390625,after an unsampled tail.

analyse_presence_crossings.py tracks record address plus configuration and service identity. All22AI records cross300->310;player23nevercross. Counts remain23player/22AI across the entire route at every sampled state,with45unique records and no duplicate/identity/map errors. First crossing bracket73905.2109375..73905.8203125;last73921.21875..73921.8359375. Distances reset from near the end of300 to near the end of310 and then decrease,in agreement with differing node direction. These are abstract point records,not full vehicle extents or an occupancy boolean. Native topology already shows300/310 connected throughjunction302;the kind2junction itself does not host these kind1presence lists.

The final-record crossing shares the sampled bracket73921.21875..73921.8359375 with junction302ownerAI2->null,vector300listAI2->empty/state35 1->3,and signalitems301/306associationAI2->null withflags32769->1. Their aspects remain0. This is live release corroboration of the previously traced helpers;shared brackets do not prove instruction order or exact causation.

Next bracket73921.7265625..73922.3359375:player acquires vector292/295/300 lists,all state35 3->0. Then73922.7265625..73923.34375:junction302branch0->1 andownernull->player;249branch1->0 andownernull->player;284ownernull->player withbranch0unchanged. Signal318aspect0->7 andassociationnull->player;297associationchangeswithaspect0unchanged;298aspect0->3;300aspect3->4. These are raw native aspect codes. The exact forward signal seen from the cab has not yet been joined to these itemindices,so do not claim a specific red-to-green UI correspondence from this capture alone.

Summary preserves61discrete events:5junction,7vector,15signal,34presence count/membership changes. The crossing artifact resolves those aggregate presence events into22individual records. Constraint34 did not change;release and selected-branch changes now have positive dynamic coverage. Transitions remain sequential snapshots,not atomic producer traces. Physical per-car crossing positions,player crossing,and other routes/lifecycles remain open. Inventory count stays920;this pass strengthens existing candidates.
