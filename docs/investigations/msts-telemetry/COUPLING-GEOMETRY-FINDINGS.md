# Coupling endpoints, gap and force boundaries

Current interpretation update: MOTION-DIFFERENCE-FINDINGS.md shows moving track-following player/AI can change orientation while stored angular velocity remains zero. The endpoint velocity below is the native stored-state calculation,not yet a validated derivative of actual endpoint trajectories. Its algebraic opening/closing sign must not be presented as observed connection motion without a paired endpoint-position test.

The native0062d335 connection solver uses car+a8 as its following vehicle. It calls00634b49 with current car/end0 and following car/end1, then005ad370 on each corresponding body+5c. The local endpoint formula is `(0,1-definition414,sign*definition400)`, sign=-0.5 for end0 and+0.5 otherwise. These are solver ends, not yet universal locomotive front/rear labels. Definition400 is the previously mapped vehicle length; the exact asset meaning of414 remains unbound here.

005ad370 forwards body+c as the transform to005aaa01/005aaa39. The latter explicitly computes local x*body right(c), local y*up(18), local z*forward(24), plus body position30. The solver measures the norm of the difference of the transformed endpoints, constructs a unit direction when above its small threshold, and projects the endpoint velocity difference onto that direction. Endpoint velocity helper005e17a0 is not newly traced here. No gameplay-side native calls were made.

Read-only `read_coupling_geometry.py` copies bounded vehicle chains and raw definition/body inputs, then reconstructs endpoints externally in Python double precision. `coupling-geometry-paused-01` at28919.865234375 has one physical player train400000 with9cars/8connections. All pair pointer checks passed; no simulation-time crossing. Endpoint distances range0.14941956543486126..0.15047771404266985. These are nonnegative distances between solver connection points, not signed slack or a measured spring deformation. There were no physical AI trains in this snapshot; AI applicability is a structural candidate, not sampled proof.

All9coupler-type bytes definition1d4 read1. The solver computes its force only for types1..4; other types produce a zero vector in this routine. It passes definition1e4 as its spring block. Its captured block+24 is0.14999999105930328, block+28 is1000000000, block+4 is5000000, block+8 is0, block+c is1000000 and block+10 is0. Raw bytes are retained; uninterpreted padding/flag bytes must not be given meanings.

0062d1a4 computes the spring component `(distance-block24)*block4`, adding `(distance-block28)*block8` when distance is greater than block28 plus the native epsilon. If block byte1 bit2 is set, it calculates damping from projected relative speed and block+c/+10, then limits its magnitude against the spring magnitude. This bit is set in all9captured blocks. With the bit clear, this routine branches to return without assigning its damping output; do not substitute zero or claim a valid universal force reconstruction without tracing initialization/caller invariants.0062d2ab combines the outputs and multiplies by the connection direction. These scalar helper interpretations come from assembly as well as decompilation; units and parser field names still need binding.

Stored car1a0 values on the8connected cars span4.930952072143555..2888.2177734375; the unconnected final car has0. Existing0062ad6a code stores a norm-derived force magnitude for the current-to-a8 connection and uses the preceding car's1a0 for the opposite side. This is a shared connection quantity, not two independent signed end forces. Its live entry is already patched, as documented in pass26. Static consumer code does not prove every current runtime branch or solver phase. No numerical identity between reconstructed spring force and stored1a0 is asserted.

Pass1567functions1182instructions4132bytes match disk/live. Pass15714functions2503instructions9144bytes match disk;3live instruction-range mismatches occur only at additional exported caller006283c0/1/3 (jump patch/padding). The directly used005ad370,0062d1a4 and0063a279 ranges match. Pass1582functions48instructions129bytes and pass1593functions93instructions226bytes match disk/live. Byte checks cover exported ranges, not all callers or runtime physics.

No UI input, coupling change, memory write, asset edit or save overwrite occurred. Further work: bind spring parameters to asset tokens, reconstruct endpoint relative velocity and signed extension with solver precision/phase limitations, test traction/compression transitions, and compare physical AI. Station boarding/departure work remains open independently.

## Endpoint velocity and signed separation rate

005e17a0 computes endpoint velocity as body88 linear velocity plus the cross product of body94 angular velocity and `(world endpoint-body30 position)`. Assembly passes endpoint/body position to subtract helper005e1561, then angular velocity and displacement to cross helper005e15b5, then adds linear velocity with005e150d. No assumed sign convention was substituted for these helpers.

In0062d335,0062e634 subtracts current endpoint from following endpoint, and likewise subtracts current endpoint velocity from following endpoint velocity.0062e80f is a dot product. Thus the scalar passed as relative speed to the spring/damper routine is `(v_following-v_current) dot unit(p_following-p_current)`. For nondegenerate geometry it is a separation rate, positive opening and negative closing, not signed force or accumulated slack. At distance <= native753e38 the native direction is zero. The new probe retains the live threshold, sampled1.0000000116860974e-7. This constant was read live here, not independently compared with disk in this pass.

Immutable `coupling-velocity-paused-01` again reads9cars/8connections with stable pairs at paused28919.865234375. All angular velocities are zero; linear velocities include tiny residual values despite native train speed0. Derived rates range-1.233244272010497e-10..9.106190681454629e-11. These retained near-zero numbers are not evidence of visible motion, meaningful compression/extension, or a tested nonzero angular contribution. The updated probe uses double precision externally and is not an exact reproduction of float/x87 intermediate rounding or solver-phase sampling. Earlier captures retain their original probe copies.

Pass160 is a preserved failed export: unquoted PowerShell address0062e634 became Infinity, rejected by the exporter before a complete export. It is not evidence for native meanings. Quoted-address pass160b completed18functions5492instructions21463bytes with0disk mismatches,6live instruction-range differences confined to known patched entries006283c0 and0062ad6a (3ranges each),0read errors. Direct endpoint/subtract/dot helper ranges match. Pass1614functions159instructions441bytes match disk/live. All jobs terminal; no UI/game writes. Next validation needs movement and/or nonzero angular velocities, not another paused-zero sample.
