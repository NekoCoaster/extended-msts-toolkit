# Receiver list and definition metadata

Native0053f153 walks sentinel=[global7c32f0+14a], starting at[sentinel], following node+0 until the sentinel, and reading receiver pointer at node+8. `read_event_receivers.py` reproduces that traversal with a4096-node bound, cycle rejection, per-object registry identity checks and end-of-read link/context checks. It never calls native receiver code. The exact-image Reader opens query/read access only.

The probe reads receiver+4 handle and verifies registry[handle*8]==receiver. It records class, raw flags, masks, current/previous scalars and last-processing tick using earlier native evidence. Definition=[receiver+10]; stream count is definition+10, and indexed trigger-state slot count is definition+0c. Receiver+14/+18 point to the corresponding allocated runtime states. The counts describe configuration/allocation, not currently audible streams, active triggers or queued events. They have conservative probe bounds; these bounds are not native capacities.

`receiver-list-paused-01` finds20 receivers, all class67, with48 total declared streams. End-of-read list/registry checks pass at paused simulation time36193.43359375. Nine receivers match player car sound handles: eight car+25c handles and the lead+4b6 handle. All inspected nonzero car handles occur in the receiver list. The rear locomotive's additional+4b6 is zero in this session. No AI ownership is established by this Explore Route snapshot.

`receiver-list-labels-paused-01` repeats the same20/48/9 result and follows the string source used by native debug formatter0053af56: [[definition+4]+4], bounded to512 UTF16 code units. All20 strings terminate and decode successfully. These are loaded definition labels, not guaranteed unique filenames or absolute paths, and do not by themselves establish a physical owner.

Observed labels include AcelaEng.sms for the front/rear car+25c receivers, six AcelaWag.sms, and AcelaCab.sms for lead+4b6. The eleven receivers without matches through the inspected car fields have labels USATrack0Ex.sms, USATrack0In.sms, AcelaPas.sms, clear_in.sms, clear_ex.sms, ingame.sms, sta_dc_lite2.sms, gen_dc_ind1.sms (two instances), sta_dc_ful2.sms and gen_dc_res2.sms. Their labels supply targeted asset/lifecycle research leads; no inferred owner is promoted to a verified association.

The loaded AcelaCab.sms label strengthens the cab-receiver interpretation for this particular lead+4b6 object. AcelaEng.sms identifies its other definition. This does not establish a universal slot-to-role mapping for every engine, vehicle or session. Duplicate gen_dc_ind1.sms labels also demonstrate why label alone is insufficient as runtime identity.

These snapshots are non-atomic. List stability checks do not exclude remove/reinsert ABA, and definitions or scalar contents can change during a read. Audio processing was previously observed to continue while the simulation is paused. Absence of a car association is not absence of an owner, and no live AI/audio lifetime conclusion follows from this session. Next inspect the labelled definitions and trigger mappings, then repeat association/lifecycle tests with physical AI and changing camera or location. Preserve identity reuse and stale-state caveats.

## Squared distance and configured thresholds

Pass125 (27 instructions/74 bytes) and126 (52/133) trace00543636 and its vector helpers. They subtract the three receiver coordinates at+58/+5c/+60 from manager listener coordinates at+14e/+152/+156, then sum squares without a square root. The outer processing routine stores that result at receiver+38 only when receiver+1c bit0x4 is clear. This field is squared native coordinate distance, not a linear distance. Coordinate units/origin are not independently established by this audio-only trace. All inspected instruction bytes match disk/live.

`receiver-distance-paused-01` repeats the stable20-receiver list at paused36193.43359375 and preserves both positions and loaded activation/deactivation flags and thresholds. `analyse_receiver_distance.py` reconstructs squared distance using float32 differences. Among19 receivers without bit0x4,13 reconstructed stored values match exactly; the maximum absolute discrepancy is0.5 at a stored distance near4.88million. Arithmetic precision and asynchronous audio updates remain limitations; no exact all-row equality is claimed. The ingame.sms receiver has bit0x4 set and stored distance0 despite nonzero geometric separation, confirming why that branch's validity condition matters.

Loaded definition+1c/+28 are distance thresholds used with activation/deactivation flag bits. AcelaCab.sms reads10000, AcelaEng.sms1000000 and AcelaWag.sms3025. Targeted installed files declare distances100,1000 and55 respectively, supporting squared loaded thresholds. These are configuration values, not measured distances, and a zero threshold is not proof of zero range when its corresponding flag disables the comparison. AcelaPas.sms has no Distance declaration and reads zero thresholds.

`receiver-distance-summary.json` preserves SHA256 and all literal Distance/Streams declarations for the four inspected Acela SMS files. It does not resolve scalability groups or parse the complete sound schema. The loaded cab/engine/wagon/passenger stream counts9/8/1/1 agree with available declarations, but lower scalability groups also exist. No claim that every installed group is active is made. These four new asset hashes are a separate targeted evidence set; the original126-file inventory source manifest is unchanged.

New candidate fields preserve receiver/listener position vectors, stored squared separation and loaded activation/deactivation squared thresholds. Camera/location transitions, origin changes, exact unit mapping, flag-state transitions and independent AI receivers remain unvalidated.

## Cab/external/cab activation test

Normal game controls switched from cab to external camera2, then restored cab1, pausing for each read. `analyse_receiver_camera.py` compares receiver-distance-paused-01 at36193.43359375, receiver-external-camera-01 at36206.50390625 and receiver-cab-restored-01 at36224.8046875. All list checks pass, the same20 handles remain present and all20 complete flag words return to their original values. Twelve change during the external view. Eighteen receiver positions remain identical; the two differences are retained in the summary rather than assuming every source stays fixed when the train is stationary.

Pass127 traces005414f1 setting receiver+1c bit0x2 (21 instructions/57 bytes, disk/live match). Previously traced005414b8 clears that bit.0053f153 selects these paths using loaded activation/deactivation conditions including camera mode and squared distance. The observed bit therefore supports an inactive-condition state, not proof that a sound buffer is stopped or inaudible; the processing loop can still examine triggers while this bit is set.

AcelaCab.sms and USATrack0In.sms switch bit0x2 from clear to set in the external view and clear again in cab. The front/rear AcelaEng.sms and several AcelaWag.sms receivers do the opposite. Not all wagons activate: their distance thresholds remain relevant. clear_in.sms/clear_ex.sms similarly exchange the bit. Two other labelled sources also change as listener position moves. This does not isolate camera-only versus distance-only causation for every receiver.

Listener position changes from[638.232421875,24.613201141357422,-37.0494384765625] to[684.3838500976562,36.71320343017578,-65.87544250488281] in external view. No transition latency or playback status is measured by these separated snapshots. Train controls remain idle/forward/pantograph up with brakes applied; final game state is paused in the restored cab. AI activation and receiver creation/removal remain untested.
