# Station record times and regenerated distances

Player station processor00586f58 uses activity+80 as its service and service+34 as selected record. It writes global day clock80acd4 into record08 on the accepted arrival/boarding branch (005875a2), sets record1e bit2 and enables activity370 with a duration at374. It writes clock80acd4 into record10 on departure-related branches, including005874da, and sets record1e bit8. These are recorded arrival/departure candidates, not universally physical sensor timestamps.

Record08 can also be copied from record04: one branch handles a passed platform with zero platform3c, and another uses global793f14. The latter clears793f14 afterward and sets record1e bits2/20. Therefore the first Maryland record08=28801, equal to declared arrival, does not prove arrival was observed at that exact gameplay time. The snapshot first-record flags1e=0x22 are consistent with this behavior but do not independently prove which writer executed. Record10 was0 for both stops; no departure transition has yet been captured.

Record0c is compared against80acd4 when checking readiness after the wait; values match declared DepartTime. The full schedule-loading path still needs tracing before universally asserting record04/0c provenance. Processor374 is decremented by frame delta828fb4 while370 is nonzero; expiration clamps to0, sets record1e bit20 and clears370. Flag meanings are branch-local and incomplete; AI uses a separate service driver path and is not validated by this player routine.

Path-list builder0064e602 traverses route geometry via0064e310 using callback0064e64e. The callback allocates/zeroes28 bytes, assigns a platform ID, writes the computed distance argument to record14, assigns sequential low16-bit index to1c, defaults24 to0.75 and clears1e. This establishes a native distance recomputation path consistent with the Maryland differences from file text; it is not a numerical reimplementation of the whole route traversal. Activity merging and selected mode determine use of rebuilt versus service-file records.

Pass1501034instructions3931bytes,pass151260/835,pass152749/2757 all match disk/live. Existing maryland-records-paused-01 raw words retain record08=28801/0 and record10=0/0, with unchanged simulation28809.5703125. No new game input, boarding, departure or AI-stop transition occurred in this pass. Next: trace schedule assignment and capture a controlled station-state transition. Preserve scheduled, recorded and potentially synthesized times as distinct semantics.

## Scheduled arrival/departure assignment and merge

005a1656 parses token4040d ArrivalTime into temporary record+4 (destination005a1749, float-reader call005a175a), and token4040f DepartTime into record+c (destination005a178e, call005a179f). Both native label/token pairs match disk/live; station-schedule-dispatch.json verifies the two jump-table entries at005a1afa against live memory. This assembly check avoids relying on this function's unreliable decompiled stack variables.

After parsing,005a1656 calls005a102e with mask1a: source temporary list, destination service30. The merge requires matching record0 platform IDs and, for bit2, equal signed16-bit skip counts at1c. Mask bits8/10 copy source4->destination4 and sourcec->destinationc and set destination20 byte bit2. If the returned match count differs from the temporary list count, it calls again with1c: same platform ID and strict source distance within destination distance +/-1 instead of skip count equality. This fallback is not guaranteed unique; native code chooses the first match in destination order.

Thus record4 is scheduled arrival and recordc scheduled departure in the traced activity traffic loader. Maryland record values28801/30607 and28811/30627 match declarations exactly. They are distinct from recorded8/10 and synthesized arrival behavior. Timing offset editing, missing declarations, AI timetable population and later schedule mutation remain unvalidated. Pass1532600instructions9458bytes match disk/live; no game state changed.

## Stationary schedule crossing and cue latch

`read_station_series.py` pairs day clock, pause state, player speed, activity boarding state and bounded lists for player/AI services. It requests only QUERY_LIMITED_INFORMATION and VM_READ, rejects unsupported executable hashes, copies source into each new capture directory and preserves raw record bytes. Observations are sequential; pointer checks and unchanged clock do not establish an atomic snapshot.

`maryland-station-baseline-01` contains one paused sample at28809.5703125. `maryland-station-wait-01` contains879 samples,0errors,97paused and170clock crossings. It spans28809.5703125..28889.744140625. The first stop changed flags1e from0x22 to0x62 between observations28810.96875 and28811.072265625, straddling scheduled departure28811. Player speed stayed0, boarding-active370 and remaining374 stayed0, recorded departure10 stayed0, and selected first record111689260/effective efficiency0.75 did not change. F10 showed Baltimore, scheduled and displayed actual arrival08:00:01, scheduled departure08:00:11, blank actual departure and loading00:00.

Assembly005877ac..00587817 checks flag40 clear, scheduled departure strictly less than current day clock, and either flag20 set or platform3c zero. It then attempts a dispatch with EDX0x82 and stack argument0x3c, and sets bit40 irrespective of the dispatch return. Thus bit40 is a departure-cue dispatch-attempt latch in this player branch, not proof of an audible cue, signal clearance, permission to proceed or movement. 0058ea72 resolves a callback through an object registry and can return0 without invoking one. The exact callback/observable effect is not established here.

005a5254 moves service34 into previous38, calls00690280 with ECX=service30 and EDX=current record, stores the result in34 and clears bit40 on a nonnull new selection. The list helper itself remains to be traced; no live selection advance occurred.

`maryland-station-departure-01` is named for the intended test, not a successful departure. Its1463 samples have0errors,992paused and108clock crossings, spanning28871.62109375..28919.865234375; it overlaps the end of the wait capture. All samples have speed0. Two cab-lever drag attempts produced no visible brake reduction; one semicolon press changed displayed train brake67% to65%. No throttle/reverser input or movement followed. Both captures have0 flagged service/list stability failures. Neither validates nonzero boarding countdown, actual departure or second-record selection/efficiency. Raw evidence and analysis remain distinct from intended actions.

Pass154 checks1080instructions3996bytes:0disk mismatches,5live instruction-range mismatches,0read errors. All mismatches are in additional exported caller00586293 at00586293/94/96,005862dc and00586449; do not claim pass-wide live equality. The directly used00586f58,0058ea72 and005a5254 ranges match disk/live. NEMT source runtime/signal.h at published651591a includes a call replacement at00586449, consistent with that observed modification; this is not a complete attribution of all five differences or validation of the patched caller behavior. No patches were added by these probes.

Game remains paused at28919.865234375 with brakes applied, no save overwritten. Next: use reliable keyboard control to release brakes and capture movement/selection, then test a true boarding countdown. AI populated stops and broader infrastructure/physics discovery remain open.

## Identity-based successor and null ambiguity

Pass155 resolves00690280. ECX is the sentinel and EDX the current record identity. With EDX0, an empty list returns0 and a nonempty list returns the first node's record8. With a nonnull identity it traverses nodes, chooses the first matching record8, then returns the following node's record8 unless that node is the sentinel. End-of-list or missing identity returns0. It does not compare schedule times, skip indexes or path distances, and supplies no cycle/capacity guard for a malformed list.

Together with005a5254, advance copies selected34 to previous38, replaces selected34 with this successor, and clears the departure-cue latch40 on the new record if nonnull. A subsequent call starting with selected0 would select the first record again; this does not prove any gameplay caller actually does so. Null selection is therefore ambiguous between an empty list, exhausted list and missing identity; do not encode it as unconditional route completion.

Pass155 exported5functions930instructions3184bytes,all disk/live equal. `station-successor-model.json` uses the already captured two Maryland record identities to illustrate five boundary cases without invoking native code or writing game memory. It is an offline model, not a live transition validation. No UI input or game modification occurred in this pass.

The three service inventory entries now replace outdated empty-list, unknown-loader and unknown-time caveats with current evidence and remaining limitations. Historical finding sections above remain chronological; the generated inventory represents the consolidated current interpretation.
