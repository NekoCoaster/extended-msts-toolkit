# Event evaluator and mutable activation state

Evaluator0059a7d3 walks activity+324 only when the list exists and activity+348 is zero. Gate0059b76f tests event+24 as a signed integer greater than zero. If enabled and event+10 is clear, dispatcher0059b083 evaluates the event predicate, stores its result into+10 and invokes0059ae8e when nonzero. Thus+10 is a trigger latch, not a universal completed-objective counter. If+10 and reversible+6a are nonzero, the predicate is reevaluated even without the positive-activation gate; a false result clears+10 and runs0059ad81. The assembly supports this ordering.

The positive outcome handler0059ae8e resolves target event IDs through0059c430 (outcome record+4 against event+c). Outcome1 increments target+24,2 decrements it,3 sets it to1 only if below1, and4 restores it from target+20. Therefore+24 is mutable current activation, while+20 is its reset baseline. The reader now uses signed reads for both fields. Reversal handler0059ad81 decrements for1, increments for2, clears a positive level for3, and has no equivalent reversal for4 through7 in this function. Reversal is not a general undo of all side effects.

Outcome6 sets activity+348=1 and+34c=1. Outcome7 sets+348=1,+34c=0, updates activity+3b8 through a string-related helper and ORs failure flags+378 with20000 hex. Existing writer/asset evidence identifies6 as success; the opposite outcome path supports a failure interpretation for7, but actual result-display/lifecycle transitions remain untested. Record+34c together with+348; zero alone does not mean failure. These fields do not replace the game's UI lifecycle or every failure path, especially with the pre-existing NEMT activity-end modifications.

Location predicate0059b146 uses the first player car at train+62 and its position+170, converts the event tile/offset through0059e3ca, ignores vertical separation, and requires horizontal squared distance strictly below radius squared. TriggerOnStop adds a transformed train-speed comparison through0071c57f against double007706d8; the helper and threshold still need tracing before reproducing that stop predicate. This is player-based, not an AI location event. Time predicate0059b24c compares event+6e against global0080acd0 with a strict less-than test; the clock's meaning is not established here and must not be confused with observed simulation clock0080acd4.

Fresh read-only snapshots activity-evaluator-paused-01/02 at74600.2734375 show all three location event latches0, reset/current activation1 and reversible0. Stop and success fields both read0. No event was triggered and no game input was performed in this pass.

Native verification: pass64 has10 functions/1206 instructions/4167 bytes; pass65 has4/247/866; pass66 has7/484/1600. All recorded instruction bytes match disk and live process, with zero errors. pass63 repeated the earlier absolute event-list reference export and added no evaluator evidence; scanning the activity-region displacement324 located the object-relative evaluator instead. Raw displacement hits alone were not treated as functions or fields.

Two candidates are added for evaluation-stop and event-outcome-success; existing event fields are refined without duplicating them. Remains: actual latch/outcome/reset/reversal transitions, exact location stop threshold and coordinates, time-event clock, other action predicates, result consumers and operational evaluation counters. This is an incomplete local checkpoint after published2978ac2.


## Location predicate reconstruction

Pass67/68 resolves the previously open helper:0071c57f clears the double sign bit for finite ordinary inputs, giving absolute speed. The threshold at007706d8 is double0.10000000149011612 (about0.1m/s), equal on disk/live.0059e3ca subtracts signed origin tiles0079d118/1c, converts through0059e414 to float32, multiplies by float2048 at0076e48c and adds the event offset.0059e264 subtracts vectors. The strict radius test is horizontal; vehicle body center and camera position are not substitutes for the first car's+170 position.

probe_event_conditions.py reconstructs ordinary finite conditions without invoking native code. event-conditions-paused-01 at74600.2734375 records stable origin/player pointers and time. Horizontal distances for event IDs2,3,0 are approximately11966.309,12783.874,12471.504m, all well outside their10/50/99m radii. Speed0 satisfies the stop threshold but does not satisfy the spatial predicate. Reconstructed predicates are false, consistent with their observed clear latches; this is not proof that the native evaluator ran during pause or that the reconstruction matches every boundary case. The script preserves float32 stores but does not fully emulate x87 precision, overflow or nonfinite behavior.

Global0080acd0, consumed by time predicate0059b24c, reads800.2717895507812 while simulation time minus activity start is800.2734375. The near match suggests elapsed activity seconds but is not exact; do not replace one with the other or label the producer resolved.004fae92 consumes the difference between these globals, and00587c3e copies0080acd0 to its context+1d0. No producer was found among the direct references. This clock is retained as a raw candidate with explicit uncertainty.

Pass67 verifies26 exported functions/4003 instructions/14095 bytes; pass68 verifies2/38/102. All recorded instruction bytes match disk/live, zero errors. Only the relevant helper/consumer paths described here have been reviewed semantically. New candidates are the time-event comparison clock, derived horizontal event distance and derived location condition. These are not additional independently read native predicate outputs. Origin transitions, near-radius boundaries and actual event firing remain untested. Files remain local after2978ac2.

Clock follow-up: CLOCK-FINDINGS.md traces the independent accumulator producer/reset and identifies NEMT live timing patches. The earlier unresolved-producer statement is superseded; runtime reset/midnight and stock timing remain untested.
