# Shared Default parameters used by evaluation

Correction: global80aa1c is a shared simulation object, not a reference car. Loader00494850 allocates0x98 bytes and constructs00628fcb with0x195 before storing it at80aa1c. Constructor00628fcb looks up the wagon definition named Default through006229a5 and stores it at object+94. Fresh live name is Default. Earlier captures' reference_car/reference_definition keys are historical mistaken labels; raw evidence is unchanged. Current probe uses simulation_object/default_wagon_definition.

Installed TRAINS/TRAINSET/DEFAULT/default.wag declares25 quoted ExtraParameters. Native token table795660/795664 maps ExtraParameters to4017b; parser00614c8e subtracts40164, reads selector617120+index, and dispatches through6170d8. Token4017b selects3 and branch616a70. That branch reads25 values into definition+770+4*index. Serializer006171f8 traverses the same25 offsets. Selected live token/dispatch data are checked by map_default_parameters.py; instruction ranges are separately verified.

Indices23 and24 therefore map to+7cc and+7d0. The asset labels them durability max acceleration and its reciprocal, with source values29.4 and0.034013605442176. Both exactly match the loaded float32 values29.399999618530273 and0.03401360660791397. default-parameters-map.json retains source SHA256, all25 source expressions/comments and loaded values. Only23/24 receive numerical matching; other expressions are not executed or their meanings independently validated.

Evaluation00586666 uses the two loaded values independently: if abs(car stored acceleration)<=ceiling, it multiplies by scale; otherwise its threshold is1. It appends only when loaded car Durability is strictly below the threshold. For the observed finite nonnegative range and rounded defaults, the threshold is at most1, so the observed Durability1.0 cannot satisfy this strict comparison. Do not claim a future arbitrary default.wag modification preserves that bound or that the reciprocal is dynamically recomputed. Nonzero evaluation testing needs suitable existing activity/consist configuration; no asset was modified in this pass.

These are loaded shared configuration parameters, not independently simulated per-car acceleration limits. Scope across all physics/evaluation consumers and live mutation/reset is not exhaustively validated. Both fields are added as individually named runtime-accessible candidates, while the existing configuration ExtraParameters entry remains source provenance rather than a unique additional live-value claim.

Validation: pass87 constructor132 instructions/512 bytes; pass88 lookup45/120; pass89 parser/serializer3454/12947, all disk/live instruction checks match. Pass86 exported four exploratory functions; its raw-immediate search hits for7cc were branch-displacement coincidences, not writes to those fields. No whole-pass86 verification claim. Fresh default-threshold-paused-01 reads both intact consists and unchanged paused simulation74030.6796875. No game input or asset/production change occurred. Remaining work includes other Default parameters' physical consumers, alternate configuration coverage and nonzero evaluation transitions.
